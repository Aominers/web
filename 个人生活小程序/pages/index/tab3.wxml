<view class="weui-grids">
  <navigator class="weui-grid" aria-role="button"  wx:for="{{ grids }}" wx:key="index"url="{{item.url}}">
    <view class="weui-grid__icon">
      <image src="../../images/icon_tabbar.png" />
    </view>
    <view class="weui-grid__label">{{ item.label }}</view>
  </navigator>
</view>
